{% extends 'base.html.twig' %}

{% block body %}
    <h2>{% trans %} homepage.tags.title {% endtrans %}</h2>
    <ul>
    {%  for tag in tags %}
        <li><a href="{{ path('list_task_tag', {id: tag.id}) }}"> {{ tag.title }} </a></li>
    {% else  %}
        <li> {% trans %} tag.empty_list {% endtrans %} </li>
    {% endfor %}
    </ul>
{%  endblock %}